# CLI reference

The full `hlplot` command tree, generated directly from the live Click app
via [sphinx-click](https://sphinx-click.readthedocs.io/). Every flag below
is what the installed package actually accepts — if you upgrade the
package, this page updates automatically on the next docs build.

```{eval-rst}
.. click:: HarrisLabPlotting.cli.main:cli
   :prog: hlplot
   :nested: full
```
